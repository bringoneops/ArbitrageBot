# .trunk/trunk.yaml
version: 0.1

cli:
  # Latest stable Trunk CLI
  version: 1.25.0   # Aug 12, 2025

plugins:
  sources:
    - id: trunk
      uri: https://github.com/trunk-io/plugins
      ref: v1.6.8     # latest stable plugins tag

lint:
  enabled:
    - clippy
    - rustfmt
    - taplo

# .trunk/trunk.yaml
version: 0.1

cli:
  # use a modern CLI; 1.5.x is too old
  version: 1.21.0

plugins:
  sources:
    - id: trunk
      ref: v1.6.1
      uri: https://github.com/trunk-io/plugins

lint:
  enabled:
    - clippy
    - rustfmt
    - taplo
